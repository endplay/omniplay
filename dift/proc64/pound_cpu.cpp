#include <stdio.h>
#include <fcntl.h>
#include <stdlib.h>
#include <assert.h>
#include <unistd.h>
#include <errno.h>


int main(int argc, char *argv[]){
    int rc = -1;
    do {
    }while (rc <= 0);
    fprintf(stderr, "okay, we *could* attach to %d now\n",rc);

    return 0;
}

